# AI SDK v5 + editkit example

Streams edits from `gpt-4o` and applies them as each one completes.

```bash
# from the repo root:
bun install
OPENAI_API_KEY=sk-... bun run --filter editkit-example-ai-sdk-v5 demo

# or directly:
cd examples/ai-sdk-v5
OPENAI_API_KEY=sk-... bun run demo
```
